import 'package:flutter/material.dart';

class TopupsScreen extends StatelessWidget {
  const TopupsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Top-ups')),
      body: const Center(child: Text('Top-ups Screen - Coming Soon')),
    );
  }
}
